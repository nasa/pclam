import setuptools

# Install with "pip install -e ." from this directory
setuptools.setup(
    name="pclam",
    version="0.1.0",
    description="Code developed to calculate lineloads from CFD surfaces",
    packages=["pclam"]
)
